{
  "name": "subscribe-handler",
  "version": "1.0.0",
  "description": "Lambda function to handle subscription form submissions",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1490.0"
  }
}
